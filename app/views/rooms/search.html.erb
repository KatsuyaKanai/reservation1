<h3>検索結果</h3>
<div class="search-result"> 
    <table>
        <% @rooms.each do |room| %>
            <tr>
            <%= hidden_field_tag room.id %>
            <td><%= link_to image_tag("#{room.room_image.url}", class:"room-post-image"), controller: 'rooms', action: 'show', id: room.id %></td>
            <td><%= link_to "#{room.room_name}", controller: 'rooms', action: 'show', id: room.id  %></td>
            <td><%= room.room_introduction %></td>
            <td>¥<%= room.room_price %></td>
            </tr>
        <% end %>
    </table>
</div>